import { styled } from "@mui/material";

export const NoServicesViewWrapper = styled("div")(() => ({
  boxSizing: "border-box",
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  width: "100%",
  gap: "2.5rem",
}));

export const ButtonWrapper = styled("div")(() => ({
  width: "20rem",
}));
